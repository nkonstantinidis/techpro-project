// Server component for metadata
export const metadata = {
    title: 'Live Chat | Realtime Collaboration App',
    description: 'Live Chat',
  };
  
  export default function ChatLayout({ children }) {
    return children;
  }