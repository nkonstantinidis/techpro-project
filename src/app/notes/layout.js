// Server component for metadata
export const metadata = {
    title: 'Notes | Realtime Collaboration App',
    description: 'Notes',
  };
  
  export default function NotesLayout({ children }) {
    return children;
  }